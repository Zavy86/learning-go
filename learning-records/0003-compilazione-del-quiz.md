# Compilazione del quiz

Lo Studente ha compilato con successo il quiz usando `make build` ed ha eseguito il binario prodotto in `bin/quiz`.

## Evidenza

Il comando `file bin/quiz`, eseguito il 2026-08-20, ha restituito:

```text
bin/quiz: Mach-O 64-bit executable arm64
```

## Implicazioni

Lo Studente sa distinguere l'esecuzione immediata di `go run` dalla compilazione persistente con `go build`. Il prossimo concetto del task attivo sono gli argomenti della riga di comando con `os.Args`.
